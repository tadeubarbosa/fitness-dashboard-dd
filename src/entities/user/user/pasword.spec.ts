describe('Password', () => {
    test('should receive the exactly password', () => {});
    test('should throw an error when a empty password has been passed', () => {});
    test('should throw an error when a invalid password has been passed', () => {});
});