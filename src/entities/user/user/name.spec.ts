describe('Name', () => {
    test('should receive a valid name', () => {});
    test('should throw an error when a empty name has been passed', () => {});
    test('should throw an error when a invalid name has been passed', () => {});
});