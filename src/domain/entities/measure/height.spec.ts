describe('Height', () => {
    test('should receive a valid height', () => {});
    test('should throw an error when a invalid height has been passed', () => {});
    test('should throw an error when a invalid height has been passed', () => {});
});