describe('CreateAnAccount', () => {
    test('should receive an valid data', () => {});
    test('should throw an error when an invalid data has been passed', () => {});
    test('should create an account', () => {});
});